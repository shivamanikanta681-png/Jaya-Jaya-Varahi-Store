rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /products/{productId} {
      // Anyone can view products
      allow read: if true;
      // Prevent unauthorized client-side writes. 
      // Owners must use a secure backend or Firebase Auth to modify catalog.
      allow write: if false; 
    }
  }
}
